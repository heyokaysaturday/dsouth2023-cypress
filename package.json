{
  "name": "dsouth2023-cypress",
  "scripts": {
    "cypress": "cypress run --browser chrome",
    "cypress:headless": "cypress run --headless --browser chrome",
    "cypress:open": "cypress open",
    "cypress:new-screenshots": "cypress-image-diff -u"
  },
  "devDependencies": {
    "@cypress/snapshot": "^2.1.7",
    "cypress": "^12.11.0",
    "cypress-dotenv": "^2.0.0",
    "cypress-image-diff-js": "^1.23.0",
    "cypress-mochawesome-reporter": "^3.4.0",
    "pixelmatch": "^5.3.0",
    "pngjs": "^7.0.0"
  }
}
